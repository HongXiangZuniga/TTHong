package IStarModel.Contribution;

import CommonModel.Relation;

public class Make extends Relation {
    public Make(String origin, String target) {
        super(origin, target, "Relation");
    }
}
